rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /{document=**} {
      // ログイン済みユーザーにのみ、読み取り・書き込みを許可（POC環境用）
      // 本番環境では各コレクションごとに厳密なルールを設定する必要があります
      allow read, write: if request.auth != null;
    }
  }
}